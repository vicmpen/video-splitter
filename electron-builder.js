// electron-builder.js
exports.default = {
    // afterCopy: [
    //   (buildPath, electronVersion, platform, arch, callback) => {
    //     if (platform === 'darwin') {
    //       const { execSync } = require('child_process');
    //       const ffmpegPath = path.join(buildPath, 'ffmpeg-bin/ffmpeg', 'ffmpeg');
    //       const ffprobePath = path.join(buildPath, 'ffmpeg-bin', 'ffprobe');
          
    //       console.log('Setting executable permissions on:', ffmpegPath);
    //       console.log('Setting executable permissions on:', ffprobePath);
          
    //       execSync(`chmod +x "${ffmpegPath}"`);
    //       execSync(`chmod +x "${ffprobePath}"`);
    //     }
    //     callback();
    //   }
    // ]
  };